apiVersion: v1
kind: Service
metadata:
  name: spacetimedb
spec:
  type: {{ .Values.service.type }}
  selector:
    app: spacetimedb
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}